from decimal import Decimal

first_num = Decimal(input("Enter first number: "))
second_num = Decimal(input("Enter second number: "))

sum = first_num + second_num
print()

print(f"{first_num} + {second_num} = {sum}")
